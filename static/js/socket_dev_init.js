socket('9c79f14df986a1ec693c', {
  mode: 'enquiry',
  element: '#socket-enquiry'
});

socket('9c79f14df986a1ec693c');

socket('9c79f14df986a1ec693c', {
  mode: 'enquiry-modal',
  element: '#socket-enquiry-modal'
});
